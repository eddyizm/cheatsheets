// Inline IF statement 

condition ? first_expression : second_expression;

/*boolean*/ condition ? /*if true this one is triggered */ first_expression : /*else the second one*/ second_expression;


// msdn example setting a variable
classify = (input > 0) ? "positive" : "negative";

// checking if value equals 1, return first value , else return second value
return (value == 1 ? Periods.VariablePeriods : Periods.FixedPeriods);